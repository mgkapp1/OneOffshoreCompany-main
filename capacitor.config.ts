import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.sleepychinchillazoom',
  appName: 'sleepy-chinchilla-zoom',
  webDir: 'dist'
};

export default config;
